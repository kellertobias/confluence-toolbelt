# Local environment file
.env

node_modules/
docs/
dist/
.DS_Store

# Obsidian plugin build output (canonical manifest.json + versions.json live at
# the repo root and ARE committed; plugin-dist/ is regenerated by build:plugin).
plugin-dist/
